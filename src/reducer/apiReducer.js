import { api_call } from "../action"
import {apiCall} from "../action/taskAction"
  const INITIAL_STATE = {
    Users:"",
  };
console.log(apiCall,"nopeeee")
  export default (state = INITIAL_STATE, action) => {
    //  console.log("task reducer"  , action);
return state
    
};